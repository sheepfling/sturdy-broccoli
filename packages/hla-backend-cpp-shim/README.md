# HLA C++ Shim Backend

This package holds the C++ shim contest surface for `hla.rti1516e`.

The Python package exposes route metadata and a generic loader/adapter for
Python extension modules or shared libraries. The `cpp/` tree contains the
native kernel and pybind/grpc scaffolding.
