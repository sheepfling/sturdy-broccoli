# Portico RTI Package Migration

Implementation moved into:

- `hla.vendors.portico.real_rti_portico`
- `hla.vendors.portico.plugin`

Remaining cleanup:

- Decide whether generic Java bridge adapters should be renamed out of the
  Pitch package names.
