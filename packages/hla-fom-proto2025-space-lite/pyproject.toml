[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hla-fom-proto2025-space-lite"
version = "0.13.0"
description = "Proto2025 SpaceLite showcase runner package for the workspace"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "hla-rti1516e==0.13.0",
  "hla-rti1516-2025==0.13.0",
  "hla-backend-common==0.13.0",
  "hla-backend-python1516e==0.13.0",
]

[project.optional-dependencies]
test = ["pytest"]

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]
include = ["hla.foms.proto2025_space_lite*"]
namespaces = true

[tool.hla.package]
status = "implementation-moved"
role = "fom-example"
source_roots = [
  "packages/hla-fom-proto2025-space-lite/src/hla/foms/proto2025_space_lite",
]
