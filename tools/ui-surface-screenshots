#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
source "$ROOT_DIR/scripts/lib/shell.sh"
hla2010_shell_init "tools/ui-surface-screenshots"

exec "$(hla2010_shell_python_bin)" "$ROOT_DIR/scripts/generate_ui_surface_screenshot_packet.py" "$@"
